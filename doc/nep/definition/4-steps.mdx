# 步骤

[//]: # (This file is automatically generated by script, do not modify it.)

import { Tag } from "../../components/tag.tsx"
 
通过步骤组成工作流完成指定操作。 
## Copy
复制文件/文件夹。
### 字段
#### from
源路径，支持相对路径和绝对路径，支持使用通配符。
* 类型：`String`  
#### to
目标路径，支持相对路径和绝对路径。
* 类型：`String`  
#### overwrite
<Tag>可选</Tag> 是否覆盖，缺省为 `false`。
* 类型：`bool`  
## Delete
删除文件/文件夹。
### 字段
#### at
删除目标路径，支持相对路径和绝对路径，支持使用通配符。
* 类型：`String`  
#### force
<Tag>可选</Tag> 是否强制删除，缺省为 false。
* 类型：`bool`  
## Execute
执行自定义命令
### 字段
#### command
需要执行的命令，使用终端为 cmd。
* 类型：`String`  
#### pwd
<Tag>可选</Tag> 执行目录，缺省为包安装目录。
* 类型：`String`  
#### call_installer
<Tag>可选</Tag> 当前命令的语义是否为正在调用安装器，缺省为 `false`；请务必正确指定此项，因为这会影响包权限、工作流静态检查等行为。
* 类型：`bool`  
#### wait
<Tag>可选</Tag> 枚举值：`Sync` `Delay` `Abandon`，缺省为 `Sync`。

`Sync`：同步等待命令执行完成后该步骤才会结束；

`Delay`：异步执行命令并立即完成当前步骤；在当前工作流执行完成时等待该命令执行结束，然后才会结束工作流；

`Abandon`：异步执行命令并立即完成当前步骤；在当前工作流执行完成时若此命令还未结束则直接强行停止此命令。
* 类型：`String`  
## Kill
杀死某个进程。
### 字段
#### target
进程名称，大小写敏感且必须以 `.exe` 结尾。
* 类型：`String`  
## Link
创建快捷方式。
### 字段
#### source_file
源文件路径，支持相对路径和绝对路径。
* 类型：`String`  
#### target_name
<Tag>可选</Tag> 快捷方式名称，支持使用 `FOLDER/NAME` 的模式表示在创建位置的目录中放置快捷方式。
* 类型：`String`  
#### target_args
<Tag>可选</Tag> 快捷方式的启动参数。
* 类型：`String`  
#### target_icon
<Tag>可选</Tag> 快捷方式图标。
* 类型：`String`  
#### at
<Tag>可选</Tag> 创建位置，是一个枚举值数组，枚举值： `Desktop` `StartMenu`。
* 类型：`Vec<String>`  
## Log
打印日志。
### 字段
#### msg
日志内容。
* 类型：`String`  
#### level
<Tag>可选</Tag> 日志级别，枚举值：`Debug` `Info` `Warning` `Error` `Success`，缺省为 `Info`。
* 类型：`String`  
## Move
移动文件/文件夹。
### 字段
#### from
源路径，支持相对路径和绝对路径，支持使用通配符。
* 类型：`String`  
#### to
目标路径，支持相对路径和绝对路径。
* 类型：`String`  
#### overwrite
<Tag>可选</Tag> 是否覆盖，缺省为 `false`。
* 类型：`bool`  
## New
新建文件/文件夹。
### 字段
#### at
新建位置，以 `/` 结尾表示新建一个目录。
* 类型：`String`  
#### overwrite
<Tag>可选</Tag> 是否覆盖，缺省为 false。
* 类型：`bool`  
## Path
将可执行文件/文件夹暴露到 PATH 目录中。
若指定一个可执行文件，则会在统一管理的 bin 目录中创建一个入口；
若指定一个文件夹，则会将其添加到 PATH 变量中。
### 字段
#### record
记录值，支持可执行文件/目录，支持相对路径和绝对路径。
* 类型：`String`  
#### alias
<Tag>可选</Tag> 别名，仅对可执行文件生效，缺省为原文件名。
* 类型：`String`  
## Rename
重命名文件/文件夹。
### 字段
#### from
目标路径，支持相对路径和绝对路径。
* 类型：`String`  
#### to
新的名称。
* 类型：`String`  
## Toast
弹出消息通知。
### 字段
#### title
消息标题。
* 类型：`String`  
#### content
消息内容。
* 类型：`String`  
## Wait
等待一个指定的时间。
### 字段
#### timeout
等待的时长，单位为 ms。
* 类型：`u64`  
#### break_if
<Tag>可选</Tag> 若满足指定条件则提前结束等待，该条件会在等待过程中每 500ms 检查一次。
* 类型：`String`  