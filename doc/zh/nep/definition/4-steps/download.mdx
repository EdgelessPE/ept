# Download

[//]: # (This file is automatically generated by script, do not modify it.)

import Tag from "../../../components/tag.tsx"
 
（仅能在拓展工作流中使用）从 URL 下载文件并使用提供的 BLAKE3 Hash 校验完整性。 

## 字段
### url
下载地址。
* 类型：`String`  
* 示例：`url = "https://github.com/BLAKE3-team/BLAKE3/releases/download/1.5.4/b3sum_windows_x64_bin.exe"` 
* 校验规则：
  * 是合法的 HTTP 或 HTTPS 地址
### hash_blake3
使用 BLAKE3 计算得到的哈希值。
* 类型：`String`  
* 示例：`hash_blake3 = "2646a295ef814f070b188278e11ee321d9c6fa18fb9e5bf4b11fcf05b6ef4ff6"` 
* 校验规则：
  * 长度为 64 位，且由数字、小写字母、大写字母构成
### to
保存的相对位置，起始目录为包内容目录（和包名相同的目录）
* 类型：`String`  
* 示例：`at = "bin/b3sum.exe"` 
* 校验规则：
  * 是合法的相对路径
  * 在校验工作流时不存在该文件 
## 权限
* 类型：[download_file](/nep/definition/3-permissions.html#download_file)
* 等级：[Important](/nep/definition/3-permissions.html#important)
* 目标：取字段 `url` 的值