# General Fields

[//]: # (This file is automatically generated by script, do not modify it.)

import Tag from "../../../components/tag.tsx"

The definition of common fields attached to steps.
## name
<Tag>Optional</Tag> The name of the step, defaulting to the sentence case of the step key.
* Example:

  ```toml
  name = "Create Shortcut"
  ```

## step
Step type
* Validation rule: Must be one of the values defined in [Steps](/nep/definition/4-steps/0-general.html).
* Example:

  ```toml
  step = "Link"
  ```

## if
<Tag>Optional</Tag> The condition for step execution.
* Validation rule: It must be a valid condition.
* Example:

  ```toml
  if = "Exist(\"./mc/vsc.exe\") && IsDirectory(\"${SystemDrive}/Windows\") || Exist(\"${AppData}/Roaming/Edgeless/ept\")"
  ```
