# General Fields

[//]: # (This file is automatically generated by script, do not modify it.)

import Tag from "../../../components/tag.tsx"
 
Common field definitions attached to steps.
## name
<Tag>Optional</Tag> Step name, default to the sentence case of the step key.
* Example:

  ```toml
  name = "Create Shortcut"
  ```

## step
Step type
* Validation rule: Must be one of the values defined in the step.
* Example:

  ```toml
  step = "Link"
  ```

## if
<Tag>Optional</Tag> Condition for step execution.
* Validation rule: Must be a valid condition.
* Example:

  ```toml
  if = "Exist(\"./mc/vsc.exe\") && IsDirectory(\"${SystemDrive}/Windows\") || Exist(\"${AppData}/Roaming/Edgeless/ept\")"
  ```
